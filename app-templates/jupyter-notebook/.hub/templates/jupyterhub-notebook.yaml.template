apiVersion: agilestacks.com/v1alpha1
kind: JupyterHubNotebook
metadata:
  name: ${application.name}
spec:
  image: ${component.docker.image}
  token: ${application.token}
  githubRepo: ${application.git.remote}
  githubToken: ${application.git.token}
