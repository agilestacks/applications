---
version: 1
kind: application
meta:
  name: argo-workflows:1
  brief: >
    Argo worfklow application with minio bucket trigger and a GitHub
  source:
    repository: https://github.com/agilestacks/applications.git
    repoDir: app-templates/argo-workflows
    branch: master
    fromEnv: GITHUB_TOKEN

components: []

lifecycle:
  bare: allow
  verbs: [deploy, undeploy]

parameters: []
templates:
  files:
    - "*.template"
