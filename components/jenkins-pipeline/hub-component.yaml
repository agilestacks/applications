---
version: 1
kind: component
meta:
  name: jenkins-pipeline
  brief: Create resources for jenkins operator

parameters:
- name: component.jenkins-pipeline
  parameters:
  - name: application
  - name: stateFile
  - name: stateFileS3
  - name: deployKey
  - name: repositoryUrl
  - name: credentialsName
    value: ${component.jenkins-pipeline.application}_CREDENTIALS

templates:
  files: ["*.template"]
